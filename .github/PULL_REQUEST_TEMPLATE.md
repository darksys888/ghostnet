## Summary

<!-- What does this change, and why does it need to ship? -->

## Test plan

<!-- How was this verified? Tick what applies; describe what doesn't fit. -->

- [ ] `npm run check` passes locally
- [ ] Relevant Cypress suite(s) pass (`e2e:devcontainer` / `e2e:retro` / `e2e:cdn` / `e2e:hostnames`)
- [ ] Touched JSON config? Companion `<config>.json.md` updated.
- [ ] Touched a Retro submodule? `npm run retro:release:diff` reflects the new SHAs.

## Screenshots / output

<!-- Drop screenshots, log excerpts, or `before / after` curl output here if useful. -->
