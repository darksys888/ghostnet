{
  "name": "tests-retro",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tests/retro",
  "projectType": "application",
  "tags": ["scope:tests", "type:e2e", "target:retro"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "tests/retro",
        "command": "cypress run"
      }
    },
    "e2e:open": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "tests/retro",
        "command": "cypress open"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "tests/retro",
        "command": "tsc --noEmit"
      }
    }
  }
}
